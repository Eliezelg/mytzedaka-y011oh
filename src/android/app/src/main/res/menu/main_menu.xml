<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layoutDirection="locale"
    android:supportsRtl="true">

    <group
        android:id="@+id/group_profile"
        android:checkableBehavior="none"
        android:orderInCategory="1"
        android:visible="true"
        android:enabled="true">

        <item
            android:id="@+id/menu_edit_profile"
            android:icon="@drawable/ic_edit_profile"
            android:title="@string/profile_edit_profile"
            android:contentDescription="@string/content_desc_edit_profile"
            android:tooltipText="@string/profile_edit_profile"
            android:orderInCategory="1"
            app:showAsAction="ifRoom" />

        <item
            android:id="@+id/menu_donation_history"
            android:icon="@drawable/ic_history"
            android:title="@string/profile_donation_history"
            android:contentDescription="@string/content_desc_donation_history"
            android:tooltipText="@string/profile_donation_history"
            android:orderInCategory="2"
            app:showAsAction="never" />
    </group>

    <group
        android:id="@+id/group_settings"
        android:checkableBehavior="none"
        android:orderInCategory="2"
        android:visible="true"
        android:enabled="true">

        <item
            android:id="@+id/menu_payment_methods"
            android:icon="@drawable/ic_payment"
            android:title="@string/profile_payment_methods"
            android:contentDescription="@string/content_desc_payment_methods"
            android:tooltipText="@string/profile_payment_methods"
            android:orderInCategory="3"
            app:showAsAction="never" />

        <item
            android:id="@+id/menu_settings"
            android:icon="@drawable/ic_settings"
            android:title="@string/profile_settings"
            android:contentDescription="@string/content_desc_settings"
            android:tooltipText="@string/profile_settings"
            android:orderInCategory="4"
            app:showAsAction="never" />
    </group>

</menu>